# vitte runtime (tokio-like) — arborescence complète

Ce dossier contient un squelette complet du runtime Vitte (executor + reactor + io/net/time + mem/sync/task),
avec manifests `.muf` et fichiers `.vitte` placeholders.
